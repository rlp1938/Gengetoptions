<OPTION>
<comment>
	Just write all text between text tags.
</comment>
<text>
	<name>HELP</name>
	<shortname>h</shortname>
	<longname>help</longname>
	/* variables in scope of gopt.c. If there are none delete everything
	   between opening and closing tags.
	   Repeat the group for as many variables as you want.
	*/
	<goptvariable>
		<gname>
		</gname>
		<type>
		</type>
	/* Write the C code to set the default value here.*/
<default>
</default>
	</goptvariable>
	<optvariable>
		<vname></vname>
		<type></type>
	/* Always just type C code between 'default' tag pairs
	   Format with tabs as required to place the code where you want in
	   the resulting C program.
	   The variable name must be prefixed with 'opts.'
	*/
<default>
</default>
	</optvariable>
	<optarg>0</optarg>
	/* Always just type C code between 'code' tag pairs as many
	   lines as neeeded.
	   Format with tabs as required to place the code where you want in
	   the resulting C program.
	   If you are coding options_t vars, the names must be prefixed with
	   'opts.'
	*/
<code>
		dohelp(0);
</code>
<helptext>
	Print this message and exit.
</helptext>
</text>
</OPTION>
